<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.findme"> <!-- এখানে আপনার প্রজেক্টের সঠিক package name দিন -->

    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindMe"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDk30wyWrbDQifpIzNoK-n7Jl9v-RIDEBY" />

        <!-- Launcher Activity: AuthActivity -->
        <activity
            android:name="com.example.findme.ui.auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.findme.ui.auth.SignUpActivity" />
        <activity android:name="com.example.findme.ui.friendlist.FriendListActivity" />
        <activity android:name="com.example.findme.ui.profile.MyProfileActivity" />
        <activity android:name="com.example.findme.ui.map.GoogleMapActivity" />

    </application>

</manifest>